# Tuples are immutable objects
# They are similar to lists but it can't be changed
t = (1,3.5,"a")
print(len(t))
#------------------------
print(t[0])
#------------------------
print(t[-1])
#------------------------
# we can't assign or append as they are immutable