# I could import the name module in another way if i don't want to import all the functionalities
# here we imported only this function and we can use it directly without module name
from name import fname


n = fname("Sleman")
print(n)
